spring:
  datasource:
  jpa:
    properties:
      javax:
        persistence:
          validation:
            mode: none
  profiles:
    active:
      - dev
---
  profiles: prod
---
  profiles: dev
  spring:
    datasource:
      generate-unique-name: false
      url: jdbc:h2:./devdb
      username: sa
    jpa:
      hibernate:
        ddl-auto: update